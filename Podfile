# Uncomment this line to define a global platform for your project
platform :ios, ‘8.1’

target 'FavoriteManager' do
  # Uncomment this line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for FavoriteManager

  target 'FavoriteManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FavoriteManagerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
